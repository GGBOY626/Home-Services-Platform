spring:
  datasource:
    url: jdbc:mysql://localhost:3306/home_services?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: app
    password: appsecret
    driver-class-name: com.mysql.cj.jdbc.Driver

app:
  seed-passwords-update: true

logging:
  level:
    com.homeservices: DEBUG
    org.hibernate.SQL: DEBUG
